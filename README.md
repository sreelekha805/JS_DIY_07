# DOM Manipulation
**The TASK :**
+ Use template literals to display City: Delhi, Temp: 30°C.
+ Fetch data from a free weather API using async/await.
+ Show an error message if the API call fails.
___
### First:
Creating the strings and add them using the template literals.

### Second:
Created an asynchronus function to fetch the data from the weather API.
+ If it can't fetch the data, the function will give error.

### Third:
Created a function to display the data which is fetched from the API called `displayData`. Which will take the variable, where the data is fetched, and it will create html elements to display the data one by one within a loop.
___
